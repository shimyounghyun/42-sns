type User {
  id: Int!
  email: String!
  verifiedAuth: Boolean!
  userName: String!
  profilePhoto: String!
  bio: String
  password: String!
  createAt: String!
  updatedAt: String
}

type Query {
  user: User
}
