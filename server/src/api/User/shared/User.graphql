type User {
  id: Int!
  email: String!
  verifiedAuth: Boolean!
  userName: String!
  profilePhoto: String!
  password: String!
  createAt: String!
  updatedAt: String
}

type Query {
  user: User
}
