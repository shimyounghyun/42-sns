type AddTripResponse {
  result: Boolean!
  error: String
}

type Mutation {
  AddTrip(
    title: String
    caption: String
    file: [String]
    placeId: Int!
    datesId: Int!
  ): AddTripResponse!
}
